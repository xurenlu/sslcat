# 缓存优化总结

**日期**: 2025-01-XX  
**优化内容**: BigCache 分片优化 + 共享缓存实例

## ✅ 已完成的优化

### 1. BigCache 分片优化

**修改文件**: `internal/cache/memory_cache.go`

**变更**:
- 分片数从 **32** 降到 **16**
- 减少分片预分配内存开销

**预期收益**: 节省约 **50-100MB** 内存（分片预分配）

### 2. 共享缓存实例

**修改文件**:
- `internal/web/server.go` - 创建共享缓存实例
- `internal/web/compression_cache.go` - 支持共享实例
- `internal/imageopt/optimizer.go` - 支持共享实例

#### 2.1 创建共享缓存实例

**配置**:
- 名称: `shared_cache`
- 最大条目: **400** (压缩200 + 图片200)
- 总大小: **300MB** (50MB + 256MB)
- 单个最大: **2MB**
- TTL: **24小时**
- 清理间隔: **5分钟**（统一）

#### 2.2 键前缀区分

- **压缩缓存**: `compression:filepath:algorithm`
  - 例如: `compression:/static/js/app.js:gzip`
- **图片缓存**: `image:path_w{width}_h{height}_q{quality}_f{format}`
  - 例如: `image:/images/photo.jpg_w800_h600_q80_f_webp`

#### 2.3 向后兼容

- 保留了 `NewCompressionCache()` 和 `NewOptimizer()` 独立实例构造函数
- 新增 `NewCompressionCacheWithCache()` 和 `NewOptimizerWithCache()` 支持共享实例

**预期收益**: 节省约 **100MB** 内存（减少一个 BigCache 实例）

## 📊 总优化效果

| 优化项 | 优化前 | 优化后 | 节省 |
|--------|--------|--------|------|
| BigCache 实例数 | 2 | 1 | - |
| 分片总数 | 64 (32×2) | 16 | 75% |
| 预分配内存 | ~200MB | ~50MB | **~150MB** |
| 总内存占用 | 1.34GB | ~1.19GB | **~150MB** |

**总内存节省**: 约 **150-200MB** (约 **11-15%**)

## 📝 注意事项

### 1. 清理逻辑

由于共享缓存实例，调用 `Clear()` 会清空**所有**缓存（压缩+图片）。这是预期的行为。

如果需要单独清理：
- 可以通过键前缀实现（需要遍历所有键，性能较低）
- 建议：如果配置变化，两个缓存都可能需要清理，统一清理是合理的

### 2. 统计信息

当前统计信息会混合在一起（因为共享同一个 BigCache 实例）。

如果需要分离统计：
- 可以在 `CompressionCache` 和 `Optimizer` 中维护独立的统计计数器
- 当前实现中，它们各自维护了独立的 hits/misses 计数

### 3. 清理间隔

统一为 **5分钟**（之前图片缓存是1分钟）。

影响很小，因为：
- BigCache 会自动清理过期项
- 手动清理主要在配置变化时触发

## ✅ 测试建议

1. **功能测试**:
   - 验证压缩缓存正常工作
   - 验证图片优化缓存正常工作
   - 验证键不会冲突

2. **性能测试**:
   - 监控内存使用情况
   - 检查缓存命中率
   - 观察清理操作的影响

3. **压力测试**:
   - 高并发场景下的缓存性能
   - 内存占用峰值

## 🚀 部署建议

1. 编译并测试本地构建
2. 在测试环境验证
3. 监控生产环境内存使用
4. 如果内存仍然偏高，可以考虑进一步优化：
   - 降低图片缓存大小（256MB → 128MB）
   - 减少压缩缓存条目数（200 → 100）

