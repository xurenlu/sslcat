# SSLcat CPU 高占用诊断报告

**日期**: 2025-12-27  
**服务器**: rocky@shifen.de  
**问题**: SSLcat CPU 使用率高达 281% (2.8 核心)

---

## 🔍 问题诊断

### 1. 当前状态
- **CPU 使用率**: 281.2% (PID 255648)
- **内存使用**: 779MB (4.8%)
- **运行时间**: 19小时50分钟
- **负载平均值**: 3.54

### 2. 核心问题：大量代理错误日志

**统计数据**:
- **5分钟内**: 34,404 条代理错误日志
- **1小时内**: 90,400+ 条错误日志
- **主要错误类型**:
  - `gg.some.im -> http://127.0.0.1:80: context canceled`
  - `api.myownx.com -> http://127.0.0.1:8812: connection refused`

### 3. 根本原因分析

#### 问题 1: 错误日志无限制写入
```1343:1344:internal/proxy/manager.go
proxy.ErrorHandler = func(w http.ResponseWriter, r *http.Request, err error) {
	m.log.Errorf("Proxy error %s -> %s: %v", r.Host, targetURL, err)
```

**影响**:
- 每个失败的代理请求都会产生一条错误日志
- 当后端服务不可用时，大量请求导致大量日志写入
- 日志写入消耗 CPU 和 I/O 资源
- 可能触发日志轮转，进一步消耗资源

#### 问题 2: 连接泄漏风险
- 有 26 个由 `net/http.(*Transport).dialConn` 创建的 goroutines
- 某些连接可能没有正确关闭
- 错误连接状态下的 goroutines 占用资源

#### 问题 3: 缺少错误限流机制
- 没有对相同错误的日志进行聚合
- 没有对错误日志进行速率限制
- 重复的错误会重复记录

---

## 🛠️ 解决方案

### 方案 1: 实现错误日志限流（推荐）

对错误日志进行速率限制，避免大量重复日志：

1. **添加错误日志限流器**
   - 相同错误（相同域名+目标）最多每分钟记录一次
   - 使用滑动窗口限流

2. **实现错误聚合**
   - 聚合相同错误，记录错误计数
   - 定期输出统计信息

3. **优化错误处理**
   - 使用 Debug 级别记录常见错误
   - Error 级别仅记录新出现的错误或统计信息

### 方案 2: 改进连接管理

1. **检查连接池配置**
   - 确保连接超时配置合理
   - 添加连接泄漏检测

2. **改进错误处理**
   - 确保错误时正确关闭连接
   - 添加连接状态检查

### 方案 3: 短期缓解措施

1. **禁用或降低错误日志级别**（临时）
   - 将常见代理错误降级为 Debug 或 Warn
   - 减少日志 I/O 开销

2. **检查后端服务状态**
   - 修复 `gg.some.im` 和 `api.myownx.com` 的后端服务
   - 或暂时禁用这些代理规则

---

## 📊 性能影响估算

### 当前状态
- **日志写入频率**: ~115 条/秒
- **CPU 消耗**: 估计日志写入占 50-70% CPU
- **I/O 消耗**: 持续写入日志文件

### 优化后预期
- **日志写入频率**: ~1-5 条/分钟（限流后）
- **CPU 降低**: 预计减少 50-70% CPU 使用
- **I/O 降低**: 日志写入减少 95%+

---

## 🔧 实施建议

### 立即行动
1. ✅ 检查后端服务状态，修复或禁用有问题的代理规则
2. ✅ 实现错误日志限流机制
3. ✅ 优化错误处理逻辑

### 长期优化
1. 实现完整的错误聚合和统计
2. 添加连接泄漏监控
3. 优化日志输出性能（异步日志、批量写入）

---

## 📝 已实施的代码修改

### 1. 添加错误日志限流器 (`internal/proxy/manager.go`)

**新增结构体**:
- `errorLogRateLimiter`: 错误日志限流器
  - 相同错误在时间窗口内（默认1分钟）最多记录一次
  - 自动清理过期记录
  - 统计总错误数和跳过的错误数

**修改位置**:
1. **Manager 结构体**: 添加 `errorLogLimiter` 字段
2. **NewManager**: 初始化错误日志限流器
3. **ErrorHandler (line 1343)**: 使用限流器记录代理错误
4. **ErrorHandler (line 760)**: 使用限流器记录后端错误
5. **Stop 方法**: 停止错误日志限流器

**效果**:
- ✅ 相同错误每分钟最多记录一次（从每秒115条降低到每分钟1条）
- ✅ 跳过的错误每100次记录一次统计（Debug级别）
- ✅ 预计减少 95%+ 的错误日志写入
- ✅ 预计减少 50-70% CPU 使用（日志I/O相关）

### 2. 错误日志限流策略

**限流规则**:
- 相同错误键（域名+目标+错误类型）在1分钟内最多记录1次
- 超过时间窗口后，错误会被重新记录
- 跳过的错误每100次记录一次统计信息（Debug级别）

**错误键格式**:
- 代理错误: `域名->目标:错误`
- 后端错误: `backend:后端ID:地址:错误`
- 通用错误: `generic:错误`

### 3. 性能优化

**自动清理**:
- 每5分钟清理一次过期的错误记录
- 保留窗口的2倍时间，避免频繁创建删除

**内存优化**:
- 使用 map 存储错误记录
- 自动清理过期记录，防止内存泄漏

---

## 🚀 部署建议

### 1. 编译和部署
```bash
# 本地编译
make build

# 或使用 Docker 编译 CGO 版本
make docker-cgo-extract

# 部署到服务器（根据你的部署脚本）
bash deploy-to-s2.sh
```

### 2. 监控验证
部署后监控：
- CPU 使用率（应该显著降低）
- 日志输出频率（应该大幅减少）
- 错误统计（可以通过 pprof 查看）

### 3. 进一步优化建议

如果 CPU 仍然较高，可以考虑：
1. **检查后端服务**: 修复或禁用有问题的代理规则（`gg.some.im`, `api.myownx.com`）
2. **调整日志级别**: 将日志级别设置为 `warn` 或 `error`
3. **优化连接池**: 检查连接超时配置

