version: '3.8'

services:
  spleeter:
    image: researchdeezer/spleeter:{{SPLEETER_VERSION}}
    container_name: {{APP_NAME}}_spleeter
    restart: unless-stopped
    ports:
      - "{{SPLEETER_PORT}}:8080"
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
    volumes:
      - spleeter_data:/app/models
    networks:
      - {{APP_NAME}}_network
    runtime: nvidia
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

volumes:
  spleeter_data:
    driver: local

networks:
  {{APP_NAME}}_network:
    driver: bridge

