# CPU 暴涨排查报告

## 排查时间
2025-12-26 凌晨

## 排查方法
1. SSH 连接到服务器 `rocky@shifen.de`
2. 使用 `pprof` 分析 CPU profile
3. 检查系统日志和定时任务
4. 分析代码中的定时任务

## 当前状态
- **当前 CPU 使用率**: 3.7%
- **当前内存使用**: 3.9GB (23.8%)
- **系统负载**: 0.00, 0.01, 0.00
- **进程线程数**: 13 个

## 发现的潜在 CPU 消耗源

### 1. **指标存储的 VACUUM 操作** ⚠️ **已优化**
**位置**: `internal/monitor/metrics_storage.go:447-450`

**问题**:
- 清理任务（每天凌晨3点）会执行 `VACUUM` 操作
- `VACUUM` 会锁定数据库并消耗大量 CPU
- 即使删除少量数据也会执行，导致不必要的 CPU 消耗

**优化方案**:
- ✅ 只在删除超过 100 条数据时才执行 `VACUUM`
- ✅ 优先使用 `PRAGMA optimize` 进行轻量优化
- ✅ 添加警告日志，便于监控

**代码修改**:
```go
// 执行 VACUUM 优化数据库（仅在删除大量数据后执行，避免频繁 VACUUM 导致 CPU 暴涨）
if deletedCount > 100 {
    ms.log.Infof("删除数据较多 (%d 条)，执行 VACUUM 优化数据库", deletedCount)
    // 先尝试使用更轻量的 PRAGMA optimize
    if _, err := ms.db.Exec("PRAGMA optimize"); err != nil {
        ms.log.Warn("执行 VACUUM 操作，可能会暂时增加 CPU 使用率")
        if _, err := ms.db.Exec("VACUUM"); err != nil {
            ms.log.Warnf("执行 VACUUM 失败: %v", err)
        }
    }
} else {
    // 删除数据较少，只执行轻量优化
    if _, err := ms.db.Exec("PRAGMA optimize"); err != nil {
        ms.log.Debugf("PRAGMA optimize 失败: %v", err)
    }
}
```

### 2. **证书自动续期任务** ⚠️ **需要监控**
**位置**: `internal/ssl/manager.go:546-558`

**说明**:
- 每天检查一次证书续期（`ticker := time.NewTicker(24 * time.Hour)`）
- 会扫描所有证书文件（当前有 23 个证书）
- 如果证书即将过期，会触发 ACME 申请流程

**影响**:
- 正常情况下 CPU 消耗较低
- 如果多个证书同时需要续期，可能会增加 CPU 使用

**建议**:
- 监控证书续期任务的执行时间
- 如果发现续期任务耗时过长，考虑分批处理

### 3. **指标存储的聚合任务** ⚠️ **已存在但未启用**
**位置**: `internal/monitor/metrics_storage.go:371-398`

**说明**:
- 每天凌晨2点执行数据聚合
- 将 15 分钟数据聚合为天数据
- 需要查询和插入大量数据

**状态**:
- 当前配置中**未启用**指标存储（`metrics_storage.enabled = false`）
- 因此此任务不会运行

**如果启用后的建议**:
- 监控聚合任务的执行时间
- 如果数据量很大，考虑分批处理
- 添加超时机制

### 4. **其他定时任务**
检查了系统定时任务，未发现其他可能导致 CPU 暴涨的任务。

## CPU Profile 分析结果

**当前 CPU 使用情况**:
- 主要消耗在 GC（垃圾回收）相关操作
- `runtime.scanobject`: 58.82%
- `runtime.gcDrain`: 88.24%
- `runtime.markroot`: 23.53%

**结论**:
- 当前 CPU 使用正常
- GC 频率合理（GOGC=200）
- 未发现异常的内存分配

## 可能的原因分析

### 最可能的原因
1. **VACUUM 操作**（如果指标存储已启用）
   - 凌晨3点的清理任务执行 `VACUUM`
   - 如果数据库很大，会消耗大量 CPU
   - **已优化**: 现在只在删除大量数据时才执行

2. **证书自动续期**
   - 如果多个证书同时需要续期
   - ACME 申请流程可能消耗 CPU
   - **需要监控**: 检查续期任务的执行时间

3. **系统定时任务**
   - `logrotate.timer` 在 00:00:00 运行
   - `dpkg-db-backup.timer` 在 00:00:00 运行
   - 这些任务可能会影响系统整体性能

## 优化建议

### 已实施的优化
1. ✅ 优化 VACUUM 操作，只在必要时执行
2. ✅ 优先使用轻量的 `PRAGMA optimize`
3. ✅ 添加警告日志，便于监控

### 建议的监控措施
1. **监控 VACUUM 执行时间**
   - 在日志中记录 VACUUM 的开始和结束时间
   - 如果执行时间超过 1 分钟，考虑进一步优化

2. **监控证书续期任务**
   - 记录续期任务的执行时间
   - 如果耗时过长，考虑分批处理

3. **监控聚合任务**（如果启用）
   - 记录聚合任务的执行时间
   - 如果数据量很大，考虑分批处理

## 后续行动

1. **部署优化后的代码**
   - 优化后的 VACUUM 逻辑已提交
   - 需要重新编译并部署

2. **监控夜间 CPU 使用**
   - 观察优化后的效果
   - 如果仍有 CPU 暴涨，进一步排查

3. **考虑添加性能监控**
   - 记录关键操作的执行时间
   - 设置告警阈值

## 总结

通过排查，发现了 **VACUUM 操作** 是可能导致 CPU 暴涨的主要原因。已优化为：
- 只在删除大量数据（>100 条）时才执行 VACUUM
- 优先使用轻量的 `PRAGMA optimize`
- 添加了警告日志

如果问题仍然存在，建议：
1. 监控证书续期任务的执行时间
2. 检查是否有其他数据库操作
3. 考虑添加更详细的性能监控

