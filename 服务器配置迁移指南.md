# SSLcat 服务器配置迁移指南

## 问题描述

您的服务器之前使用的是 `withssl.conf` 配置文件，现在项目已经统一使用 `sslcat.conf`。需要将服务器上的配置迁移到新的命名规范。

## 迁移步骤

### 1. 下载迁移脚本

在您的服务器上执行：

```bash
# 下载迁移脚本
curl -O https://raw.githubusercontent.com/your-repo/withssl/main/scripts/migrate-config.sh

# 或者如果脚本已经在服务器上
chmod +x scripts/migrate-config.sh
```

### 2. 执行迁移

```bash
# 运行迁移脚本
./scripts/migrate-config.sh
```

### 3. 手动迁移（如果脚本不可用）

如果无法使用脚本，可以手动执行以下步骤：

```bash
# 1. 检查当前配置文件
ls -la /etc/sslcat/

# 2. 备份现有配置
cp /etc/sslcat/withssl.conf /etc/sslcat/withssl.conf.backup.$(date +%Y%m%d_%H%M%S)

# 3. 重命名配置文件
mv /etc/sslcat/withssl.conf /etc/sslcat/sslcat.conf

# 4. 更新文件权限
chown sslcat:sslcat /etc/sslcat/sslcat.conf
chmod 600 /etc/sslcat/sslcat.conf

# 5. 更新 systemd 服务配置
sed -i 's|--config /etc/sslcat/withssl.conf|--config /etc/sslcat/sslcat.conf|g' /etc/systemd/system/sslcat.service

# 6. 重新加载 systemd
systemctl daemon-reload

# 7. 重启服务
systemctl restart sslcat
```

### 4. 验证迁移结果

```bash
# 检查服务状态
systemctl status sslcat

# 检查配置文件
ls -la /etc/sslcat/sslcat.conf

# 查看服务日志
journalctl -u sslcat -f
```

## 配置文件内容

迁移后的配置文件应该包含您的 proxy rules，例如：

```json
{
  "proxy": {
    "rules": [
      {
        "domain": "your-domain.com",
        "target": "127.0.0.1",
        "port": 8080,
        "enabled": true,
        "ssl_only": true
      }
    ]
  }
}
```

## 常见问题

### Q: 迁移后服务启动失败怎么办？

A: 检查以下几点：
1. 配置文件语法是否正确：`sslcat --config /etc/sslcat/sslcat.conf --validate`
2. 文件权限是否正确：`ls -la /etc/sslcat/sslcat.conf`
3. 服务用户是否存在：`id sslcat`

### Q: 如何恢复原始配置？

A: 使用备份文件：
```bash
cp /etc/sslcat/withssl.conf.backup.* /etc/sslcat/sslcat.conf
systemctl restart sslcat
```

### Q: 配置文件路径在哪里？

A: 标准路径：
- 配置文件：`/etc/sslcat/sslcat.conf`
- 证书目录：`/var/lib/sslcat/certs/`
- 密钥目录：`/var/lib/sslcat/keys/`
- 日志目录：`/var/lib/sslcat/logs/`

## 联系支持

如果遇到问题，请提供以下信息：
1. 服务器操作系统版本
2. 当前配置文件内容
3. 错误日志：`journalctl -u sslcat --no-pager`
4. 服务状态：`systemctl status sslcat`
