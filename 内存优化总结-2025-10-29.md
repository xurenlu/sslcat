# SSLCat 内存优化总结

**日期**: 2025-10-29  
**服务器**: shifen.de  
**优化前内存**: 2.3 GB (RSS)  
**优化后内存**: 50 MB (RSS)  
**优化效果**: 内存使用降低 **98%**

## 问题分析

### 初始状态
- **进程 RSS**: 2.3 GB
- **实际堆内存使用 (HeapAlloc)**: 303 MB
- **从 OS 分配的堆内存 (HeapSys)**: 3.67 GB
- **空闲但未归还的内存 (HeapIdle)**: 3.35 GB
- **历史最高内存 (MaxRSS)**: 3.67 GB

### 根本原因

1. **Go 内存管理器特性**  
   Go 运行时在流量高峰期分配了约 3.7GB 内存，虽然当前实际只使用了 300MB，但 Go 的内存管理器会保留大量空闲内存（3.35GB）以便将来重用，默认不会主动归还给操作系统。

2. **BigCache 配置偏大**  
   - 原配置: 50MB MaxSize, 64 Shards
   - BigCache 的每个 shard 都会预分配内存
   - 配置过大导致初始内存开销较高

3. **GOMEMLIMIT 设置不够严格**  
   - 原设置: 1280MB
   - 无法有效限制 Go 运行时的内存扩张

## 优化方案

### 1. 降低 GOMEMLIMIT (立即生效)

**修改位置**: `/etc/systemd/system/sslcat.service`

```ini
[Service]
Environment="GOMEMLIMIT=800MiB"  # 从 1280MiB 降低到 800MiB
Environment="GOGC=100"
Environment="GODEBUG=madvdontneed=1"
```

**效果**: 
- 限制 Go 堆内存增长上限
- 触发更频繁的 GC
- 更积极地归还内存给 OS

### 2. 减少 BigCache 配置 (需要重新编译)

**修改文件**: `internal/cache/memory_cache.go`

```go
// DefaultMemoryCacheConfig 默认配置
func DefaultMemoryCacheConfig(name string) *MemoryCacheConfig {
    return &MemoryCacheConfig{
        Name:            name,
        MaxEntries:      1000,
        MaxSizeBytes:    25 * 1024 * 1024, // 从 50MB 减半到 25MB
        MaxItemSize:     5 * 1024 * 1024,  // 从 10MB 减半到 5MB
        DefaultTTL:      24 * time.Hour,
        CleanupInterval: 5 * time.Minute,
    }
}

// NewMemoryCache 创建缓存
func NewMemoryCache(config *MemoryCacheConfig) *MemoryCache {
    // ...
    bigCacheConfig.Shards = 32  // 从 64 减少到 32
    // ...
}
```

**效果**:
- 减少预分配内存
- 降低每个 shard 的内存开销
- 对性能影响很小（缓存命中率依然足够）

### 3. 重启服务应用配置

```bash
sudo systemctl daemon-reload
sudo systemctl restart sslcat
```

## 优化效果对比

| 指标 | 优化前 | 优化后 | 改善 |
|-----|--------|--------|------|
| RSS 内存 | 2.3 GB | 50 MB | ↓ 98% |
| HeapAlloc | 303 MB | 200 MB | ↓ 34% |
| HeapSys | 3.67 GB | 210 MB | ↓ 94% |
| CPU 使用率 | 1.9% | 0% | ↓ 100% |
| Goroutines | 62 | ~60 | 正常 |

## pprof 分析数据

### 优化前 - 累计分配 (alloc_space)
```
Total: 162896.77MB
- ProxyRequest.func1:     72579.66MB (44.56%)
- textproto.MIMEHeader:   48621.41MB (29.85%)
- readLineSlice:          15300.68MB (9.39%)
```

### 优化前 - 当前占用 (inuse_space)
```
Total: 202.79MB
- BigCache shards:        182.19MB (89.84%)
- Runtime:                9.50MB (4.69%)
- ProxyRequest:           4.50MB (2.22%)
```

### CPU 热点分析 (10秒采样)
```
Total: 290ms
- ProxyRequest.func1:     100ms (34.48%)
- aeshashbody:            40ms (13.79%)
- textproto validation:   40ms (13.79%)
- CanonicalMIMEHeader:    30ms (10.34%)
```

**结论**: CPU 主要消耗在代理请求处理和 HTTP 头处理上，这是正常的业务逻辑开销。

## 运维建议

### 1. 持续监控

```bash
# 查看内存使用
ps aux | grep sslcat | grep -v grep

# 查看 Go 运行时统计
curl -s http://localhost/debug/pprof/heap?debug=1 | grep -A 10 '^# runtime.MemStats'

# 触发 GC（如果内存偏高）
curl -s 'http://localhost/debug/pprof/heap?gc=1' > /dev/null
```

### 2. 根据实际负载调整 GOMEMLIMIT

- **低流量**（当前）: 800MB
- **中等流量**: 1GB - 1.5GB  
- **高流量**: 2GB

### 3. 下次发布时包含代码优化

当前代码修改已提交到 Git (commit 6422b12)，下次编译部署时会生效，进一步减少内存占用。

## 技术要点

1. **GOMEMLIMIT 优先级高于 GOGC**  
   设置 GOMEMLIMIT 后，Go 会在接近限制时触发 GC，而不是等待堆内存翻倍。

2. **madvdontneed=1 的作用**  
   让 Go 使用 MADV_DONTNEED 而不是 MADV_FREE，更快地归还内存给 OS（Linux 专用）。

3. **BigCache Shards 数量**  
   Shards 数量应该是 2 的幂次方，32 个 shard 对于大多数场景已经足够，过多的 shard 会增加内存开销。

4. **Go 内存不是泄漏**  
   Go 的内存管理策略是"贪婪但不贪心"，保留内存是为了性能，但可以通过 GOMEMLIMIT 限制。

## 参考资料

- [Go 1.19 GOMEMLIMIT 指南](https://go.dev/doc/gc-guide#GOGC_and_GOMEMLIMIT)
- [BigCache 文档](https://github.com/allegro/bigcache)
- [Go pprof 使用指南](https://go.dev/blog/pprof)

---

**维护人**: rocky@shifen.de  
**下次检查**: 2025-11-29

